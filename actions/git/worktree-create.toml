description = "Create a git worktree for isolated work"
command = "sh"
args = ["-c", "dir=\"/tmp/granary-$(basename $(pwd))-{id}\"; git worktree add \"$dir\" -b granary/{id} 2>/dev/stderr && echo \"$dir\""]
